---
status: planned
depends_on: [4]
wave: 3
skills: [code-writing]
verify: [smoke]
reviewers: [code-reviewer, security-auditor, test-reviewer]
teammate_name:
---

# Task 5: Contract tests

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Написать полный набор Hardhat + chai тестов для `PaymentSplitterFactory` и `PaymentVaultImpl` (контракты из Task 4). Тесты должны покрывать всю бизнес-логику фабрики и vault, все ветки безопасности из decisions D3/D4/D8/D10–D17, и достигать **≥95% branch coverage** через `npx hardhat coverage`.

Это последняя линия защиты перед security audit (Task 13) и forked-e2e (Task 10). Любая невыявленная ветка в `register()`, `withdraw()`, `setFeeBps()` или в guard-инвариантах vault (D15/D16/D17) — это потенциальная уязвимость в money-handling коде. Тесты пишутся **после** реализации Task 4, потому что мы валидируем уже задеплоенные инварианты (не TDD на стороне контрактов: контракты были спроектированы в tech-spec, реализованы в Task 4 — здесь мы проверяем, что реализация соответствует спеку).

Использовать `MockUsdcEip3009` из Task 4 для всех сценариев с USDC-балансами. Под каждый test-suite — изолированная Hardhat fixture (`loadFixture`) с предзагруженной фабрикой, mock USDC, deployed vault impl, treasury address, owner и набором dev-EOA.

## What to do

### PaymentSplitterFactory.test.ts

Покрыть следующие группы тестов (ровно по Testing Strategy → Contract tests):

1. **`register()` — happy path и инварианты**
   - Deploys vault at predicted address (`Clones.predictDeterministicAddress` off-chain == `factory.computeVaultAddress(dev)` on-chain).
   - После register: `vaults[developer]` равен задеплоенному адресу.
   - Vault `initialize(_developer)` вызван ровно один раз и с `_developer == msg.sender` register-call'а.
   - Второй вызов `register()` от того же EOA — revert `AlreadyRegistered`.
   - Когда фабрика `paused()` — `register()` revert с OZ 5.x `EnforcedPause()`.

2. **CREATE2 cross-component invariant**
   - Off-chain хелпер (мы пишем его в тесте через viem или ethers utils, точно так же как middleware будет считать `payTo`) вычисляет адрес vault по формуле `Clones.predictDeterministicAddress(vaultImpl, bytes32(uint256(uint160(developerEoa))), factoryAddress)`.
   - Этот адрес ДОЛЖЕН byte-в-byte совпадать с тем, что вернёт `factory.computeVaultAddress(developerEoa)` on-chain.
   - Проверить минимум для 3 разных developer EOA — это инвариант, на котором держится вся off-chain маршрутизация `payTo` в middleware (D3). Если они разъедутся, middleware будет отправлять платежи на адрес, который никогда не задеплоится.

3. **`register()` reentrancy invariant**
   - Сценарий: developer EOA — это контракт, чей fallback/`initialize` пытается re-enter `factory.register()`. Это не должно ни задеплоить второй vault, ни нарушить state фабрики.
   - Атака блокируется либо OZ `ReentrancyGuard`, либо check-before-effect ordering (`vaults[msg.sender]` пишется ДО внешнего call'а в `initialize`).
   - Цель — pin инвариант для будущих рефакторингов: даже если контракт когда-то добавит callback в `register()`, тест не даст откатиться к незащищённой версии.

4. **`setFeeBps(uint16 bps)`**
   - Owner-only — non-owner получает revert (OZ 5.x `OwnableUnauthorizedAccount`).
   - `bps > 1000` (1001, max uint16) — revert `InvalidFeeBps`.
   - `bps == 0`, `bps == 1000`, `bps == 50` (default), `bps == 500` — успешно, состояние обновляется.
   - Событие `FeeBpsUpdated(oldBps, newBps)` эмитится с правильными значениями.

5. **`setPlatformTreasury(address)`**
   - Owner-only.
   - `address(0)` — revert `ZeroAddress`.
   - Happy path: меняется состояние, эмитится `PlatformTreasuryUpdated(oldTreasury, newTreasury)`.

6. **`Ownable2Step`**
   - `transferOwnership(newOwner)` сам по себе не передаёт владение — нужен `acceptOwnership()` от `newOwner`.
   - До `acceptOwnership` старый owner всё ещё owner; `setFeeBps` от newOwner revert'ит.
   - После `acceptOwnership` владение перешло; старый owner больше не может вызывать owner-only функции.
   - Сценарий cancel: `transferOwnership(newOwner)` → `transferOwnership(address(0))` (или `transferOwnership` на другого) отменяет pending передачу.

7. **`pause()` / `unpause()`**
   - Owner-only — non-owner revert (`OwnableUnauthorizedAccount`).
   - После `pause`: `paused() == true`, `register()` revert'ит.
   - После `unpause`: `register()` снова работает.

8. **Constructor reverts**
   - `_usdc == address(0)` — revert `ZeroAddress`.
   - `_treasury == address(0)` — revert `ZeroAddress`.
   - `_feeBps > 1000` — revert `InvalidFeeBps`.
   - Happy: deployed с валидными аргументами — `usdc()`, `platformTreasury()`, `feeBps()`, `vaultImpl()`, `owner()` все читаются корректно.

9. **`VaultDeployed` event + edge cases**
   - Event arg `vault` равен предсказанному CREATE2 адресу.
   - Event arg `developer` равен `msg.sender` register-call'а.
   - `vaults[unregistered_address]` возвращает `address(0)`.
   - Two distinct dev EOAs в одном Hardhat-блоке (через `network.provider.send("evm_setAutomine", [false])` + `evm_mine` после) — получают **distinct vault addresses** (CREATE2 salt различается).

### PaymentVaultImpl.test.ts

Все тесты через **clone**, развёрнутый из factory в test fixture (а не напрямую через `vaultImpl`), кроме явных D15 тестов.

1. **`initialize` single-call**
   - Первый вызов через factory.register() — success.
   - Прямой второй вызов `vault.initialize(otherDev)` после деплоя — revert `InvalidInitialization` (OZ 5.x).

2. **D15: `_disableInitializers()` на impl**
   - Получить адрес `factory.vaultImpl()` (это сам imple, не клон).
   - Прямой вызов `IVault(vaultImpl).initialize(anyDeveloper)` — revert `InvalidInitialization`.
   - Это канонический hijack-vector для cloneable контрактов; явный отдельный тест.

3. **D16: нет `receive()` / `fallback()`**
   - Отправить native value на адрес vault через `owner.sendTransaction({to: vault, value: 1n})` — revert (low-level reject, в OZ 5.x обычно generic revert).
   - На Arc USDC и есть native gas, но контракт-тесты идут на дефолтной Hardhat-цепи; мы тестируем именно EVM-инвариант "нет payable точек входа".

4. **D17: ABI-assertion — нет setters для `developer` / `factory`**
   - Прочитать compiled ABI (`vault.interface.fragments` или `artifact.abi`).
   - Assert: нет фрагмента с `name == "setDeveloper"` и аргументом `address`.
   - Assert: нет фрагмента с `name == "setFactory"` и аргументом `address`.
   - Дополнительно: нет ни одного state-changing метода кроме `initialize` и `withdraw` (для надёжности).

5. **`withdraw` — happy path и базовые ветки**
   - Подготовка: fund vault через `mockUsdc.mint(vault, gross)`.
   - `withdraw()` от не-developer — revert `NotDeveloper`.
   - `withdraw()` когда `usdc.balanceOf(vault) == 0` — revert `NoBalance`.
   - Happy path: vault разбивает `gross` на `dev_share = gross - (gross * feeBps / 10000)` и `fee = gross * feeBps / 10000`, шлёт каждую часть; balances developer и treasury растут на правильные значения; vault balance = 0.
   - Событие `Withdrawal(developer, gross, fee)` эмитится.

6. **Fee edge cases**
   - `feeBps == 0` → `fee == 0`, происходит **только один** transfer (на developer); второго (fee→treasury) НЕ происходит — проверить через mock USDC ивент-сниффер либо через assertion `treasury.balance` не изменился.
   - `feeBps == 1000` (max) → 10% от gross идёт treasury, 90% — developer.
   - Dust: `gross == 1` micro-USDC, `feeBps == 50` → `fee == 1 * 50 / 10000 == 0` (truncation) → весь 1 unit уходит developer, treasury не получает ничего.
   - `gross == 199` micro-USDC, `feeBps == 50` → `fee == 0`, всё уходит developer (boundary для truncation).
   - `gross == 200` micro-USDC, `feeBps == 50` → `fee == 1`, dev получает `199`.

7. **Fee-snapshot semantics**
   - Сценарий: deploy vault, `feeBps == 50`. Fund vault на `1000` USDC (платёж 1). Owner вызывает `factory.setFeeBps(100)`. Fund vault на ещё `1000` USDC (платёж 2). Теперь developer вызывает `withdraw()`.
   - Ожидаемое поведение: vault использует **текущее** значение `feeBps` (100) для всего gross = 2000, т.е. `fee = 20`, `dev = 1980`. Документированная семантика: fee читается at withdraw time, не snapshot'ится per payment.
   - Тест pins текущее поведение, чтобы случайное изменение в коде (например, snapshot per-payment) сразу провалило тест.

8. **Reentrancy через malicious treasury**
   - Setup: deploy mock-контракт `MaliciousTreasury`, у которого `receive()`/функция `onERC20Receive` (через USDC.transfer hook'а нет, но если кто-то добавит — let's also test через прямую попытку re-enter в withdraw flow). На самом деле основной вектор: USDC.transfer на treasury триггерит fallback контракта, который пытается вызвать `vault.withdraw()` повторно.
   - `factory.setPlatformTreasury(MaliciousTreasury)`. Fund vault на `1000` USDC. Вызвать `vault.withdraw()`.
   - Ожидаемое: re-entry от treasury в `withdraw` blocked OZ `ReentrancyGuard` — внешний withdraw либо success'ит (re-entry внутри проваливается тихо для USDC.transfer? нет — `ReentrancyGuard` revert'ит ВЕСЬ внешний вызов), либо revert'ит. Уточнить в реализации Task 4 — но любой исход должен быть детерминированным; денег "extra" атакующий не достаёт.
   - Также можно использовать malicious developer вместо treasury — re-entry через `gross - fee` transfer на developer-контракт.

9. **Withdraw работает при `paused()` factory**
   - Setup: register dev, fund vault, owner вызывает `factory.pause()`.
   - `vault.withdraw()` от developer — success. Developers никогда не блокируются от снятия (D12).

## TDD Anchor

Тесты, которые пишем ДО (или в данной задаче — единственное, что мы пишем). Файлы существуют как пустые после Task 4 — но если они там есть с заглушками, очищаем. Пишем тесты так, чтобы они валидировали уже задеплоенные контракты. Если какой-то тест неожиданно падает на корректной реализации Task 4 — это сигнал, что либо тест неверный, либо контракт отклонился от спека (тогда back to Task 4 / decision).

### PaymentSplitterFactory.test.ts

- `contracts/test/PaymentSplitterFactory.test.ts::register deploys vault at predicted CREATE2 address` — register() возвращает CREATE2-предсказанный адрес
- `contracts/test/PaymentSplitterFactory.test.ts::register populates vaults mapping` — vaults[dev] != 0x0 после регистрации
- `contracts/test/PaymentSplitterFactory.test.ts::register reverts AlreadyRegistered on second call` — идемпотентность
- `contracts/test/PaymentSplitterFactory.test.ts::register reverts EnforcedPause when paused` — pause integration
- `contracts/test/PaymentSplitterFactory.test.ts::register invariant: off-chain compute equals on-chain computeVaultAddress for 3 EOAs` — CREATE2 cross-component
- `contracts/test/PaymentSplitterFactory.test.ts::register reentrancy via malicious developer EOA is blocked` — reentrancy invariant
- `contracts/test/PaymentSplitterFactory.test.ts::setFeeBps owner-only, reverts on >1000, emits FeeBpsUpdated` — fee bounds
- `contracts/test/PaymentSplitterFactory.test.ts::setPlatformTreasury owner-only, reverts on zero, emits PlatformTreasuryUpdated`
- `contracts/test/PaymentSplitterFactory.test.ts::Ownable2Step transfer requires acceptOwnership, cancel works` — two-step
- `contracts/test/PaymentSplitterFactory.test.ts::pause and unpause are owner-only and toggle paused state`
- `contracts/test/PaymentSplitterFactory.test.ts::constructor reverts on zero usdc, zero treasury, feeBps>1000`
- `contracts/test/PaymentSplitterFactory.test.ts::VaultDeployed event arg matches predicted address`
- `contracts/test/PaymentSplitterFactory.test.ts::vaults of unregistered returns 0x0`
- `contracts/test/PaymentSplitterFactory.test.ts::two devs same block produce distinct vault addresses`

### PaymentVaultImpl.test.ts

- `contracts/test/PaymentVaultImpl.test.ts::initialize single-call, second call reverts` — Initializable guard
- `contracts/test/PaymentVaultImpl.test.ts::D15 direct initialize on impl reverts (_disableInitializers)` — hijack guard
- `contracts/test/PaymentVaultImpl.test.ts::D16 native value sent to vault reverts (no receive/fallback)`
- `contracts/test/PaymentVaultImpl.test.ts::D17 ABI does not expose setDeveloper or setFactory`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw reverts NotDeveloper for non-developer caller`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw reverts NoBalance when balance is zero`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw happy path splits gross to developer and fee to treasury, emits Withdrawal`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw with feeBps=0 sends gross to developer, no fee transfer`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw with feeBps=1000 splits 90/10`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw with gross=1 and feeBps=50 truncates fee to 0`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw fee-snapshot reads current factory.feeBps() at withdraw time`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw blocks reentrancy via malicious treasury`
- `contracts/test/PaymentVaultImpl.test.ts::withdraw works when factory is paused (developers not locked out)`

## Acceptance Criteria

- [ ] `contracts/test/PaymentSplitterFactory.test.ts` покрывает все 9 групп из секции "What to do".
- [ ] `contracts/test/PaymentVaultImpl.test.ts` покрывает все 9 групп из секции "What to do".
- [ ] `cd contracts && npx hardhat test` — все тесты проходят, exit code 0.
- [ ] `cd contracts && npx hardhat coverage` — **branch coverage ≥95% на обоих контрактах** (`PaymentSplitterFactory.sol`, `PaymentVaultImpl.sol`). Coverage report committed (или viewable).
- [ ] CREATE2 cross-component invariant тест проверяет минимум 3 EOA byte-в-byte.
- [ ] D15 test (direct initialize on impl) ОТЛИЧАЕТСЯ от clone-initialize test — это два разных сценария.
- [ ] D17 ABI-assertion test читает actual compiled ABI (через `artifacts.readArtifact` или `vault.interface.fragments`), а не hardcoded список.
- [ ] Reentrancy test на withdraw использует actually-reentering контракт (`MaliciousTreasury` или `MaliciousDeveloper`), не симуляцию.
- [ ] Fee-snapshot test pins **текущее** поведение (fee at withdraw time), документировано в comment теста.
- [ ] Все events проверяются через `expect(...).to.emit(contract, "EventName").withArgs(...)`.
- [ ] Custom errors (`AlreadyRegistered`, `InvalidFeeBps`, `ZeroAddress`, `NoBalance`, `NotDeveloper`) проверяются через `revertedWithCustomError`.

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [project.md](../../../.claude/skills/project-knowledge/references/project.md)
- [architecture.md](../../../.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](../../../.claude/skills/project-knowledge/references/patterns.md)

**Code files to modify (this task creates):**
- [contracts/test/PaymentSplitterFactory.test.ts](../../../contracts/test/PaymentSplitterFactory.test.ts)
- [contracts/test/PaymentVaultImpl.test.ts](../../../contracts/test/PaymentVaultImpl.test.ts)

**Code files to read (from Task 4):**
- [contracts/contracts/PaymentSplitterFactory.sol](../../../contracts/contracts/PaymentSplitterFactory.sol)
- [contracts/contracts/PaymentVaultImpl.sol](../../../contracts/contracts/PaymentVaultImpl.sol)
- [contracts/contracts/interfaces/IERC3009.sol](../../../contracts/contracts/interfaces/IERC3009.sol)
- [contracts/contracts/mocks/MockUsdcEip3009.sol](../../../contracts/contracts/mocks/MockUsdcEip3009.sol)
- [contracts/hardhat.config.ts](../../../contracts/hardhat.config.ts)

## Verification Steps

### Automated
- `cd contracts && npx hardhat test test/PaymentSplitterFactory.test.ts test/PaymentVaultImpl.test.ts` → all pass
- `cd contracts && npx hardhat test` → all pass (full suite)

### Smoke
- `cd contracts && npx hardhat coverage` → report shows **≥95% branch coverage** на `PaymentSplitterFactory.sol` и `PaymentVaultImpl.sol`. Если меньше — добавить недостающие branches (обычно error paths или один из feeBps boundary cases).
- Inspect coverage report: open `contracts/coverage/index.html` (или CLI summary) и убедиться, что red lines на factory/vault — это либо `unreachable`, либо явно невозможные в bytecode пути (зафиксировать в комментарии теста). Цель — нет красных uncovered branches в state-changing функциях.

## Details

**Files (this task creates):**

`contracts/test/PaymentSplitterFactory.test.ts` — новый файл.
- Использует `loadFixture` (`@nomicfoundation/hardhat-network-helpers`) для setup'а: deploy `MockUsdcEip3009`, deploy `PaymentVaultImpl`, deploy `PaymentSplitterFactory(mockUsdc, treasury, 50, vaultImpl)`.
- Подключает viem (или ethers, если Task 4 на ethers — посмотреть `hardhat.config.ts`) для off-chain CREATE2 compute. Если viem — `getContractAddress({from: factory, opcode: 'CREATE2', salt, bytecodeHash})` с правильным `bytecodeHash` для EIP-1167 минимального прокси.
- Группы организовать через `describe()` на каждую из 9 групп.

`contracts/test/PaymentVaultImpl.test.ts` — новый файл.
- Аналогичный `loadFixture` — но fixture также вызывает `factory.connect(dev1).register()` и возвращает реальный clone.
- Для D15: использовать `factory.vaultImpl()` напрямую, без register().
- Для D16: `(await ethers.getSigner(owner)).sendTransaction({to: vault.target, value: 1n})`.
- Для D17: `artifacts.readArtifact("PaymentVaultImpl")` → `artifact.abi.filter(f => f.type === "function").map(f => f.name)` → assert не содержит "setDeveloper", "setFactory".
- Для reentrancy: в той же папке `contracts/test/mocks/` или inline в `mocks/MockMaliciousTreasury.sol` (но он логически принадлежит test-fixture'у; если Task 4 не создал — создать тут в `contracts/contracts/mocks/MockMaliciousTreasury.sol`).

**Dependencies:**
- Task 4 (`depends_on: [4]`) — контракты `PaymentSplitterFactory.sol`, `PaymentVaultImpl.sol`, `MockUsdcEip3009.sol` должны быть реализованы и компилироваться.
- `@nomicfoundation/hardhat-toolbox` (chai, hardhat-network-helpers, hardhat-chai-matchers) — устанавливается в Task 2.
- `solidity-coverage` — обычно входит в `hardhat-toolbox`; если нет — установить и добавить в `hardhat.config.ts`.

**Edge cases:**
- CREATE2 предсказание EIP-1167: формула включает `keccak256(initCode)` где initCode — это минимальный прокси с хардкоженной impl-адрес-секцией. Использовать `Clones` helper из OZ-импортов **или** написать аналог на TS. Хорошее тестирование требует, чтобы вычисление было ДРУГИМ путём, не через `factory.computeVaultAddress` (иначе тест бесполезен).
- При тесте reentrancy на withdraw: malicious-контракт должен корректно реализовывать `receive() external payable` или интерфейс, который USDC.transfer тригернёт. USDC.transfer для обычного ERC-20 НЕ дёргает callback'и у получателя, поэтому reentry-вектор есть только если `MockUsdcEip3009.transfer` сам ходит куда-то (он не должен). Реальный re-entry вектор для vault — через **developer**-callback, не treasury (потому что vault сначала шлёт developer, потом treasury, и `nonReentrant` сработает если developer-контракт попытается вызвать withdraw). Это нужно проверить в коде Task 4 — какая последовательность transfer'ов внутри `withdraw`.
- Two-devs-same-block: Hardhat по умолчанию автомайнит каждую tx в отдельный блок. Нужно либо отключить automine (`network.provider.send("evm_setAutomine", [false])`), либо использовать `Promise.all` с явным `evm_mine`. Documented pattern.
- `EnforcedPause` — OZ 5.x кастомная ошибка; используется `revertedWithCustomError(factory, "EnforcedPause")`.
- `OwnableUnauthorizedAccount` — OZ 5.x ошибка с аргументом `account` — `revertedWithCustomError(factory, "OwnableUnauthorizedAccount").withArgs(nonOwner.address)`.
- Fee math с большими числами: использовать BigInt/`bigint` в JS, а не `number`. Hardhat-ethers возвращает `bigint` по умолчанию.

**Implementation hints:**
- Структура каждого test файла: `describe("PaymentSplitterFactory", () => { describe("register", () => {...}); describe("setFeeBps", () => {...}); ... })`.
- `loadFixture(deployFactoryFixture)` в начале каждого `it()` для дешёвого snapshot/revert (не передеплоить каждый раз).
- Для CREATE2 off-chain compute: документ [EIP-1167](https://eips.ethereum.org/EIPS/eip-1167) даёт точный bytecode pattern; OZ `Clones._cloneCreationCode()` тоже даёт его. Можно использовать `ethers.getCreate2Address(factoryAddress, salt, keccak256(initCode))`.
- Для assertion на отсутствие функции в ABI: `const abi = (await hre.artifacts.readArtifact("PaymentVaultImpl")).abi; expect(abi.find(f => f.type === "function" && f.name === "setDeveloper")).to.be.undefined;`.
- Для coverage threshold: `solidity-coverage` поддерживает `.solcover.js` с `mocha: { reporter: 'spec' }` и можно настроить fail-on-coverage-below. На этом этапе достаточно проверить в верификации.
- Не используем `loadFixtureAsync` (deprecated) — `loadFixture` синхронный wrapper.
- Hardhat node при тестировании контрактов: дефолтный `hardhat` network, не `localhost`.

## Reviewers

- **code-reviewer** → `work/x402-agent-payment/logs/working/task-5/code-reviewer-{round}.json`
- **security-auditor** → `work/x402-agent-payment/logs/working/task-5/security-auditor-{round}.json`
- **test-reviewer** → `work/x402-agent-payment/logs/working/task-5/test-reviewer-{round}.json`

## Post-completion

- [ ] Записать краткий отчёт в decisions.md по шаблону (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось (например, если тест выявил, что какой-то decision реализован иначе)
