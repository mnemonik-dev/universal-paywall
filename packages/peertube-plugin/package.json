{
  "name": "peertube-plugin-universal-paywall",
  "version": "0.0.1",
  "description": "Per-view onchain settlement for PeerTube via the Universal Paywall rail (action:api.video.viewed -> facilitator charge).",
  "engine": {
    "peertube": ">=6.0.0"
  },
  "keywords": [
    "peertube",
    "plugin",
    "universal-paywall",
    "x402",
    "payments"
  ],
  "library": "./dist/main.js",
  "staticDirs": {},
  "css": [],
  "clientScripts": [],
  "translations": {},
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild main.js --bundle --platform=node --format=cjs --target=node18 --outfile=dist/main.js",
    "prepublishOnly": "npm run build",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
